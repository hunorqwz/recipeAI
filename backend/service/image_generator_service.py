import uuid

# from config import IMAGE_GENERATOR_PIPELINE


# async def generate_image(receipt_description: str) -> str:
#     """Generate an image based on the receipt description."""
#     image = IMAGE_GENERATOR_PIPELINE(receipt_description).images[0]
#     image_path = f"public/images/{uuid.uuid4()}.png"
#     image.save(image_path)
#     return image_path

